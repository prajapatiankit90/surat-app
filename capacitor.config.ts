import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darshana.jardosh',
  appName: 'Darshana Jardosh',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
